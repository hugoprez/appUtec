import 'package:flutter/material.dart';

class ConsultarMaterias extends StatelessWidget {
  const ConsultarMaterias({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      
      children: [
        Container(
          child: Text("Interfaz de Hugo Antillon"),
        )
      ],
    );
  }
}